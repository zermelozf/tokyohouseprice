import { Routes } from '@angular/router';

// Production stub: the local-only scraper dashboard is excluded from deployed
// builds. Swapped in for dev-routes.ts via angular.json `fileReplacements`.
export const devRoutes: Routes = [];
